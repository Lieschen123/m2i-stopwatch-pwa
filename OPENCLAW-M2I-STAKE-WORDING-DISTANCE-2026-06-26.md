# M2I Stake Wording And Distance Report - 2026-06-26

## Status

Implemented and verified locally. Commit/push/deploy blocked by environment/tooling permissions.

## Requirements

- Replace open-challenge payment-request wording with missed-challenge stake wording.
- Clarify optional distance goal wording and verify logic/tests.
- Preserve public share redaction for stake/payment details.
- Run test/build/deploy checks and record results.

## Initial Inspection

- Required verification ran:
  - `ls -la OPENCLAW-M2I-STAKE-WORDING-DISTANCE-2026-06-26.md`
  - `head OPENCLAW-M2I-STAKE-WORDING-DISTANCE-2026-06-26.md`
- Existing dirty/untracked files before edits included `BUILD-REPORT.md`, `scripts/dev-server.mjs`, `.openclaw/`, workspace identity docs, prior reports, `deploy/`, and this report.
- Relevant wording found in `src/main.js`, `src/challenge.js`, `src/payment.js`, and expectations in `tests/core.test.mjs`.
- Existing distance logic in `src/challenge.js` already rejects workouts below `challenge.minDistanceKm`.

## Code Changes

- `src/main.js`: changed create/open challenge copy to "Distance goal, optional", "Stake if missed", "Missed-challenge stake", and "No stake configured"; added exact help text that distance is optional and both minutes and distance are required when set; updated final/private claim copy and button copy to stake instructions.
- `src/challenge.js`: changed invite text summary to "Stake if missed" / "No stake configured" and expanded final-review/manual settlement policy wording.
- `src/payment.js`: changed generated USDt/sats copied instructions to "Stake if missed" and "Only due if final review says the challenge was missed. If complete, no payment is due." with manual/no-custody/no-automatic-settlement wording.
- `tests/core.test.mjs`: updated wording expectations and added tests for no-stake invite text and distance goal requiring both minutes and distance.
- `dist/assets/main.js` and `dist/index.html`: regenerated by `npm run build:github-pages`.

## Verification

- `npm test` passed: 26 tests passed, 0 failed.
- `npm run build:github-pages` passed: base path `/m2i-stopwatch-pwa/`, asset version `20260626`, `dist/assets/main.js` written.
- `rg` scan found no product-source occurrences of "Manual team jar request", "manual team jar request", or open-challenge "payment request" wording in `src/`. Remaining `payment` words are internal object names, public-redaction copy, wallet URI labels, or tests.
- Public Nostr share redaction remains intact; existing tests still pass, including public claim projection redaction.

## Commit And Deploy

- Commit hash: none. Commit was blocked by filesystem permission on `.git`.
- Command attempted: `git add src/main.js src/challenge.js src/payment.js tests/core.test.mjs dist/assets/main.js dist/index.html OPENCLAW-M2I-STAKE-WORDING-DISTANCE-2026-06-26.md && git status --short`
- Blocker: `fatal: Unable to create '/Users/lieschen/Projects/m2i-stopwatch-pwa/.git/index.lock': Operation not permitted`
- Confirmed `.git` is not writable from this session: `touch .git/codex-write-test` returned `Operation not permitted`.
- README deploy command attempted: `npx gh-pages -d dist`
- Deploy blocker: `ERROR: SecItemCopyMatching failed -50`; local `node_modules/.bin/gh-pages` is not installed.
- Deployed URL/status: not deployed from this session. Expected GitHub Pages URL remains `https://lieschen123.github.io/m2i-stopwatch-pwa/` after a successful `gh-pages` deploy.

## Remaining Risks

- Custom sats manual instructions are preserved as user-provided text, but generated copied instructions now append the final-review/no-payment-due warning.
- Commit/deploy still need to be run from an environment with `.git` write access and working `gh-pages`/GitHub credentials.
- Unrelated pre-existing dirty files were not touched or reverted.

